git init
rm -rf .git
cd -
git status 
touch .gitignore
git add -A
/*working area
staging area
repo*/
git reset / git reset hello.txt
git commit -m "Inital commit"
git log
git clone
git remote -v
git branch -a
git diff
